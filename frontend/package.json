{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend for HyperLiquid ClickHouse Data Viewer",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=8080 --open=/",
    "dev": "live-server --port=8080 --open=/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["clickhouse", "data-viewer", "hyperliquid"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
